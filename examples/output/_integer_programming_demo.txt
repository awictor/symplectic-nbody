Integer programming: branch and bound over the LP relaxation

  Production planning: max 5x + 4y  s.t. 6x+4y<=24, x+2y<=6
    LP relaxation optimum: 21.00  (fractional, not buildable)
    integer optimum:       20  at x = [4, 0]
    integrality gap:       1.00
    branch-and-bound nodes explored: 5

  0/1 knapsack: 5 items, capacity 50
    values  [60, 100, 120, 80, 40]
    weights [10, 20, 30, 15, 8]
    optimal value 240, items chosen [0, 1, 3] (weight 45)
    brute force checks all 32 subsets -> value 240 (matches: True)
    branch and bound explored 17 LP nodes instead

  The LP relaxation gives a bound that lets branch and bound prune whole subtrees:
  once a subproblem's relaxation cannot beat the best integer solution so far, it
  is discarded unexplored. That is what makes exact integer optimization tractable.

  wrote examples/output\integer_programming.svg
